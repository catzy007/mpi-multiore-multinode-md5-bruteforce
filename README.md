this meant to be some test code implementing Message Parsing Interface (MPI).
to use the test code, you have to install MPICH
next compile the c file with "mpicc test.c -o test"
then run with mpiexec
